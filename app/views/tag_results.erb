<h1> <%= "Articles tagged with #{@tag.name}" %> </h1>
<ul id='tag-results'>

  <% @matching_links.each do |link| %>
    <li>
      Title: <%= link.title %>
      URL: <%= link.url %>
      Tag: <%= link.tags.first.name %>
    </li>
  <% end %>
</ul>


